import numpy as np

arr = np.array([1, 2, 3])

# Add 5 to every element
print(arr + 5) # NumPy automatically adds 5
               # To every element

#output
#[6 7 8]   # also possible substaction, divion broadcasting